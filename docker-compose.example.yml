services:
  indexer:
    build: ./indexer
    environment:
      CONTENT_DIR: /content
      OUTPUT_FILE: /data/index.json
      DEBOUNCE_MS: "1000"
    volumes:
      - ./content:/content:ro
      - index_data:/data
    restart: unless-stopped

  web:
    image: nginx:alpine
    ports:
      - "8199:80"
    volumes:
      - ./dashboard:/usr/share/nginx/html:ro
      - index_data:/data:ro
      - ./content:/content:rslave,ro
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - indexer
    restart: unless-stopped

volumes:
  index_data:
